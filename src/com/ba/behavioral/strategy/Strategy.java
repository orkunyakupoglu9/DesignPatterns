package com.ba.behavioral.strategy;

//Essentially, the strategy pattern allows us to change the behavior of an algorithm at runtime.
public interface Strategy {

    public int doOperation(int num1,int num2);
}
